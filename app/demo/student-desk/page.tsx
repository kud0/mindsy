import JsonSelector from '@/components/demo/JsonSelector';

export default function StudentDeskSelectorPage() {
  return <JsonSelector />;
}

export const metadata = {
  title: 'Flexible StudentDesk Demo - JSON Selector',
  description: 'Choose from multiple JSON files to test different lecture structures with intelligent adaptation'
};